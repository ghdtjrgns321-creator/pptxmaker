# pptmaker

내가 만든 프로젝트를 소개하는 B2B 셀링 PPT를 일관 서식으로 찍어내는 서식 공장.

## 작업 모드 — 요청이 어디에 속하는지 먼저 판별

| 모드           | 트리거                                                                                                      | 절차                                                                                                                |
| -------------- | ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| ① 덱 생산·수정 | "프로젝트 PPT 만들어줘 / 소개 자료 생성 / 발표자료 / pptx 생성 / 다시 만들어 / 슬라이드 수정 / 더 리치하게" | `pptmaker` 스킬 — **메인 대화가 직접 한다.** 질문 후보 → 갤러리 pptx → 사용자 선택 → 조판 → 렌더 눈검증 → 기계 채점 |
| ② 하네스 상속  | "이 반려를 하네스에 반영 / 이게 왜 또 나오지 / 재발 막아줘 / 규칙 정리해줘"                                 | `deck-smith` 에이전트 — 결함을 어디에 내릴지 판단해 **제안까지만**                                                  |
| ③ 그 외 전부   | 도구 제작, 스크립트 손질, 문서 작업 등 위 둘이 아니면 전부 여기                                             | 일반 코딩 작업 — 요청한 것만, 최소로. 하네스 자동 개입 금지                                                         |

단순 질문은 직접 응답 가능.

**①은 에이전트 파이프라인이 아니다**(2026-07-29 변경). 덱 만들기는 사용자와 주고받는 일이라
에이전트로 쪼개면 그 왕복이 끊긴다. 옛 4에이전트 파이프라인(deck-composer→pptx-builder→
consistency-qa)과 deck-spec 경로는 `_archive/`에 있다 — 되살리지 않는다.

## 하네스 변경 경계 (전 모드 공통)

- 하네스(스킬·게이트·계약·에이전트 정의) 변경은 **사용자가 명시 요청**했거나 **deck-smith의
  제안을 사용자가 승인**했을 때만 한다.
- 메인 대화는 하네스를 직접 고치지 않는다. 작업 중 발견한 결함·공백은 고치지 말고 **한 줄 보고**만.
- 벼리는 루프(드러난 결함을 하네스의 올바른 자리에 내려 다음 덱이 상속 — 배치 사다리:
  부품 > 게이트 > 왕복 > 반려 목록)는 **deck-smith 전용 절차**이며, deck-smith조차 **제안까지만**
  — 코드로 내리는 것은 사용자 승인 후다.

## 덱을 만들 때 어기면 안 되는 것 5개

`pptmaker/SKILL.md`가 단일 출처. 요약하면:

1. 그림 그리기 **전**에 질문 후보를 내고 사용자가 고른다
2. 고른 질문을 **전부** 시각으로 덮는다 (개수 하한이 아니라 커버리지)
3. 글자 수 하한은 유지한다
4. 부품함·골든덱은 **재료로만** 본다 — 그대로 호출 금지
5. **인코딩** 발명은 환영, **잉크** 발명은 금지

같은 파일의 **반려 목록**은 실제로 반려당한 것을 부정형으로 모아둔 것이다. 새 반려는 코드·게이트·
왕복으로 먼저 막고, 그게 안 될 때만 거기 한 줄 적는다 — **"~해라"로 적지 않는다**(한 장의 지적을
모든 장의 명령으로 승격시킨 것이 규칙이 639줄로 불어난 원인이다).

## 재료·단일 출처

- 콘텐츠 재료: 사용자가 프로젝트별로 미리 정리해둔다 — 추출 단계 없음. **어느 정리본을 쓸지는
  사용자가 지정한다**(`README.md` 완성판 / `FINAL-REPORT/` 창고 — 판단 기준은 `pptmaker/SKILL.md`).
  재료를 기계가 세는 것은 `scan_material.py`.
- 브랜드 일관성: `.claude/skills/pptx-build/assets/brand-kit.yaml` (이 파일만 고치면 전체 반영)
- 조판 도구함: `goldenfab/` — `kit`(원시도구)·`grid`(좌표계)·`dense`(밀도 부품)·`layouts`(정형 장)
- 차트·다이어그램: `pptx-visuals` 스킬 (필드는 `references/spec-fields.md`)
- 오딧 검사: `audit.py:generic_checks` 단일 출처, 러너는 `score_deck.py`
- 고정 자산: `golden/golden-deck.pptx` + `golden/render/`(17장) + `golden/render-부품/`(27장) —
  **보고 배우는 그림이지 부르는 함수가 아니다**

## 환경·의존성 (uv)

- 패키지·가상환경은 **uv**로 관리. `.venv`·`uv.lock`으로 재현 가능. Python 3.13 고정(`.python-version`).
- 스크립트 실행은 `uv run python <경로>` — venv 자동 활성, 별도 activate 불필요.
- 의존성 추가: `uv add <pkg>`(런타임) / `uv add --dev <pkg>`(개발).
  런타임 = `python-pptx`·`pyyaml`·`matplotlib`, 개발 = `ruff`·`pypdfium2`.
- `.venv`·`*.pptx`(빌드 산출물)는 커밋하지 않음(`.gitignore`).

## 레거시

`_archive/` — 2026-07-29 하네스 정리로 물러난 것 전부(코드 · 이전 워크스페이스 · 구판 렌더 ·
완료 계약 기록). **살아있는 코드가 여기를 참조하면 안 된다.** 이동 대장은 `_plan/move-*.tsv`.

## git 운영

- 브랜치 흐름 `feature→develop→main`. **main 직접 커밋 금지**, 기본 작업 브랜치는 `develop`.
- 커밋은 사용자 요청 시만. Conventional Commits, 1커밋=1논리변경, **AI/Claude 서명 금지**.

## 변경 이력

`docs/CHANGELOG.md` — 하네스 변경의 전체 이력·근거·한계 박제. **새 변경의 박제도 그 파일에 쓴다**
(이 파일에 표를 다시 만들지 않는다).
