"""goldenfab — 남은 것은 `kit`(원시도구)와 `audit`(오딧 검사)뿐이다.

2026-08-03 정리: 골든덱을 기준선에서 내리면서, 그것을 재현하던 `layouts`(정형 장)·
`dense`(밀도 부품)·`grid`(좌표계) 607줄을 지웠다. 부르는 곳이 0이었다.

**새 덱은 `deckkit`으로 만든다** — 토큰·골격(표지·목차·간지)·블록·커넥터가 거기 있다.
여기 남은 둘은 `score_deck.py`가 기존 산출물을 채점할 때만 쓴다.
"""
