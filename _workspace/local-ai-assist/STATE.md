# 진행 상태 — local-ai-assist

> 뼈대는 `scripts/deck_state.py`가 계약에서 뽑았고, **내용은 사람이 채운다.**
> 실물과 어긋나는지는 `uv run python scripts/deck_state.py check local-ai-assist` 가 본다.

## 지금 어디

- **단계**: ⑦채점   (①계약 · ②형태후보 · ③갤러리 · ④선택 · ⑤조판 · ⑥눈검증 · ⑦채점)
- **갱신**: 2026-08-03
- **다음 할 일**: 없음 — 산출 완료(`results/local-ai-assist-소개.pptx`). 미결은 아래 반려 #3·#6.

## 장별 — 무엇을 왜 골랐나

형태를 고른 뒤 채운다. `왜`가 비면 다음 덱이 같은 선택을 재현하지 못한다.

| 장 | 제목 | 고른 형태 | 왜 이걸 골랐나 | 상태 |
| --- | --- | --- | --- | --- |
| S1 | 표지 | `layouts.cover` | 정형 장 — 골든 좌표 그대로 | 완료 |
| S2 | 목차 | `layouts.toc` | 정형 장 — 골든 좌표 그대로 | 완료 |
| S3 | 간지 | `layouts.part` | 정형 장 — 골든 좌표 그대로 | 완료 |
| S4 | 문제 인식 | `anchor_card + p_cmp + p_flow` | 갤러리에서 사용자 선택 · 좌열은 2026-08-03 전면 카드로 교체 | 완료 |
| S5 | 시스템 한 장 요약 | `p_band + p_gate + p_rfan` | 갤러리에서 사용자 선택 | 완료 |
| S6 | 간지 | `layouts.part` | 정형 장 — 골든 좌표 그대로 | 완료 |
| S7 | 정상 데이터 생성 | `p_band + p_bars + p_loop + p_tape` | 갤러리에서 사용자 선택 | 완료 |
| S8 | 이상 데이터 주입 | `rich + p_chips + p_flow + p_tape` | 갤러리에서 사용자 선택 | 완료 |
| S9 | 간지 | `layouts.part` | 정형 장 — 골든 좌표 그대로 | 완료 |
| S10 | 룰 29종 | `p_bars + p_chips + p_tape` | 갤러리에서 사용자 선택 | 완료 |
| S11 | 검토 조합 | `p_bip + p_chips + p_shot + rich` | 갤러리에서 사용자 선택 | 완료 |
| S12 | 분석적 검토 | `p_split + p_shot + rich` | 갤러리에서 사용자 선택 | 완료 |
| S13 | VAE — 왜 이 모델인가 | `p_enc + p_chips + detail` | 갤러리에서 사용자 선택 | 완료 |
| S14 | VAE — 입력 설계 | `p_bip + p_cat + p_stack` | 갤러리에서 사용자 선택 | 완료 |
| S15 | 간지 | `layouts.part` | 정형 장 — 골든 좌표 그대로 | 완료 |
| S16 | 검증 ① 룰 | `p_funnel + p_bars + p_tape` | 갤러리에서 사용자 선택 | 완료 |
| S17 | 검증 ② VAE 결과 | `p_dots + p_bars + p_shot` | 갤러리에서 사용자 선택 | 완료 |
| S18 | 간지 | `layouts.part` | 정형 장 — 골든 좌표 그대로 | 완료 |
| S19 | 의사결정·한계·기술스택 | `p_cat + p_cmp + p_stack + p_tape + rich` | 갤러리에서 사용자 선택 | 완료 |

## 반려 기록 — 사용자가 무엇을 물렸나

**이 표가 다음 덱으로 넘어가는 유일한 통로다.** 여기 안 적힌 반려는 사라진다.
`하네스` 열이 `미정`인 줄이 남아 있으면 `check`가 경고한다 — 판단을 미룬 채 덮지 않기 위해서다.

| # | 날짜 | 장 | 무엇을 물렸나 | 어떻게 고쳤나 | 하네스 |
| --- | --- | --- | --- | --- | --- |
| 1 | 2026-08-02 | 전장 | 갤러리 후보들이 서로 다른 주제를 담았다 — 고를 수가 없었다 | ②단계를 "같은 내용의 형태 후보"로 재정의, 갤러리 전면 재시작 | 반영 (커밋 7bb8ab5) |
| 2 | 2026-08-02 | 전장 | 갤러리 후보 라벨이 전부 들어가 스케치가 아니라 본편이 됐다 | 후보는 스케치 — 라벨 2~3개 + "… 외 N개"로 접는다 | 반영 (커밋 ca2b603) |
| 3 | 2026-08-02 14:25 | S4 | hero_card 하나뿐이라 자리마다 안 맞는다. 카드를 여러 개 만들어놓고 골라 쓰자 | 안 함 — 바로 재조판으로 넘어갔다 | **미정** |
| 4 | 2026-08-02 14:38 | S4 | 아래 공간이 남는다 — 덮을 카드가 필요하다 | `anchor_card` 신설(로컬) | 제안함 — `dense.py` 승격 대기 |
| 5 | 2026-08-02 | 전장 | 밋밋한 즉흥 카드가 통과했다 | `check_adhoc_card` 판정 상향(좌변 바는 구조로 안 침) | 반영 (커밋 e69f978) |
| 6 | 2026-08-03 | S11 | 라벨은 "대상 9종 · 수법 10종"인데 그림은 7개 · 8개다. 헤드라인 90칸도 실제는 56칸 | 안 고침 | **미정** — 라벨 수↔도형 수 대조 검사가 없다 |

`하네스` 열에 쓸 값: `불필요`(이 덱 한정) · `제안함`(deck-smith에 올림) · `반영`(코드·게이트로 내려감) · `미정`