# 08. 시스템 전체 정리 — 무엇을 만들었고 지금 어떻게 도는가

2026-07-11 기준(Phase 2 완료 시점). 결정 배경은 [03](03-golden-deck.md)·[05](05-golden-deck-plan.md),
공장 이식 상세는 [07](07-factory-port.md).

## 정본 팩트시트 (전 수치 실측 — 아래 명령으로 재현 가능)

| 항목                | 실측값                | 산출 방법                          |
| ------------------- | --------------------- | ---------------------------------- |
| 골든 덱 슬라이드    | 19장 / 501도형        | golden-deck.pptx 파싱              |
| 골든 스크립트       | 22파일                | `ls golden/*.py`                   |
| 시안 렌더 누적      | 106장(PNG)            | `ls golden/variants/*.png`         |
| 디자인 규칙         | 불릿 83 + 번호 13     | design-rules.md grep               |
| 공장 레이아웃 타입  | 15종                  | goldenfab.registry 로드            |
| 공장 패키지         | 20파일                | `ls scripts/goldenfab/*.py`        |
| 골든↔공장 도형 일치 | **501/501, 불일치 0** | compare_golden.py (19/19 슬라이드) |
| 매칭 결정표         | 15행 전수             | layout-matching.md                 |
| 배선 검증           | 3/3 PASS              | 골든 스펙 A·B + 레거시 회귀        |

## 1. 실전 파이프라인 — "PPT 만들어줘"가 지나는 길 (현재)

```
  <프로젝트>/FINAL-REPORT/*.md                                      brand-kit.yaml (색·폰트 단일 출처)
        │                                                              │
        ▼                                                              ▼
  [① deck-outline-grill] ─▶ 01.5_outline.md ─▶ [② deck-compose] ─▶ 02_deck-spec.json
   목차·강조점 사용자 인터뷰      아웃라인 계약        물성→타입 배정          장별 type/content
   (게이트 — 대화형)                                (layout-matching.md)          │
                                                                                                            ▼
                                            [④ consistency-qa] ◀─ .pptx ◀─ [③ pptx-build  build_pptx.py]
                                             브랜드·품질 검수                    type이 "golden.*"면 ─▶ goldenfab registry(15타입)
                                                                                 그 외 레거시 타입   ─▶ 기존 렌더러
```

- ②의 타입 배정: 주제가 아니라 **물성**(비교·흐름·분해·경계·실물 화면…)으로 판정 —
  15행 결정표에 안 맞으면 끼워맞추지 않고 골든 확장 절차(pull)로 올린다.
- ③의 골든 장은 헤더·결론바·출처를 자체 포함 — 레거시 푸터 스탬프를 붙이지 않는다.

## 2. 품질 기반 — 골든 덱이 공장을 구속하는 구조 (현재)

```
  사용자 승인(시안 반려 루프, 렌더 106장)          ← Phase 1 (완료)
        │ 확정될 때마다
        ▼
  golden/*.py (22파일, 동결) ─▶ golden-deck.pptx (19장·501도형 = 정답지)
        │ 판단 박제                                   ▲
        ▼                                             │ 도형 전수 비교 (±0.005")
  design-rules.md (규칙 96항목)               compare_golden.py ── 불일치 1건이라도 → FAIL
   Ⅰ부 전역 7절 / Ⅱ부 장 유형 A~J / Ⅲ부 프로세스 P1~P4      ▲
        │ 규칙대로 이식                               │ 출력
        ▼                                             │
  goldenfab (20파일, 15타입 registry) ◀── build_pptx.py "golden.<layout>" 디스패치   ← Phase 2 (완료)
   이후 수정은 여기서만(골든은 기준 원본)
```

## 3. 구성요소 표

| 구성요소        | 위치                                         | 규모          | 역할                                 |
| --------------- | -------------------------------------------- | ------------- | ------------------------------------ |
| 골든 덱(정답지) | `golden/` (build_golden.py + 장별 스크립트)  | 22파일·19장   | 사용자 승인 디자인의 동결 기준 원본  |
| 콘텐츠 팩트시트 | `golden/00_factsheet.md`                     | 1파일         | 골든 수치 단일 출처(창작 금지)       |
| 디자인 규칙     | `pptx-build/references/design-rules.md`      | 96항목        | 박제된 판단의 단일 출처              |
| 공장 엔진       | `pptx-build/scripts/goldenfab/`              | 20파일·15타입 | 골든 레이아웃의 파라미터화 이식판    |
| 공장 본체       | `pptx-build/scripts/build_pptx.py`           | 1파일         | deck-spec → pptx (golden.* 디스패치) |
| 회귀 게이트     | `pptx-build/scripts/compare_golden.py`       | 1파일         | 골든↔공장 도형 전수 비교(501/501)    |
| 매칭 결정표     | `deck-compose/references/layout-matching.md` | 15행          | 물성→타입 판정 + pull 확장 절차      |
| 진행표          | `docs/user/07-factory-port.md`               | 19행          | 장↔타입 매핑·이식 상태               |

### 골든 덱 19장 구성 (부 6개 × 아키타입 15종)

| 부            | 장                                             | 아키타입(공장 타입)                                                           |
| ------------- | ---------------------------------------------- | ----------------------------------------------------------------------------- |
| — (앞머리)    | 1 표지 · 2 목차                                | cover · toc                                                                   |
| Ⅰ 문제 인식   | 3 간지 · 4 문제 정의                           | part · problem_grid                                                           |
| Ⅱ 파이프라인  | 5 간지 · 6 실행 그래프                         | part · exec_graph                                                             |
| Ⅲ 기술 설명   | 7 간지 · 8~12 (용어사전·그래프·화면·트리·출력) | part · tech_evidence · tech_tree · screenshot · tech_mechanism · tech_capture |
| Ⅳ 문제와 해결 | 13 간지 · 14 트러블슈팅 · 15 골든테스트        | part · ab_simulation · validation                                             |
| Ⅴ 차별점      | 16 간지 · 17 미러 매트릭스 · 18 한계(경계)     | part · mirror_matrix · boundary                                               |
| Ⅵ 성과·증거   | 19 클로징 (간지·성과 장은 이력 참조)           | closing                                                                       |

## 4. 품질 게이트 — 대충 만든 것이 사용자에게 닿지 않게 하는 4겹

| 게이트            | 시점         | 강제 방식                        | 잡는 것                          |
| ----------------- | ------------ | -------------------------------- | -------------------------------- |
| P1 물성 선언      | 그리기 전    | 계약 1번 항목, 불일치 FAIL       | 잘못된 형태로 시작               |
| P2 기계 오딧      | 렌더 전      | 스크립트 assert(빌드 실패)       | 좌표·색 상한·공기·구조 수량 위반 |
| P4 셀프 반려 체크 | 제시 전      | 반려 이력 8문항 자문, 계약 항목  | 텍스트 덤프·재탕·공백·붙음 등    |
| compare_golden    | 공장 수정 후 | 도형 501개 전수, 1건 불일치 FAIL | 골든에서의 이탈(회귀)            |

- 골든셋 사용 원칙: 골든 덱은 **아키타입 부품 창고**이지 납품물이 아니다 — 장 단위로 인출하며
  덱 내부 상호 참조 정합은 요구하지 않는다.
- 콘텐츠 파라미터화: cover·toc·part·closing·screenshot 5종 = dict 인자 / 나머지 10종 =
  골든 콘텐츠 내장(수요 시 pull — content를 주면 명시적 에러).

## 5. 남은 일 (여정 순)

1. **Phase 3** — K-IFRS 덱을 골든 스펙(deck-spec.json 19장)으로 공장에서 end-to-end 재빌드,
   실패분석([02](02-failure-analysis.md)) 갭 재점검.
2. **Phase 4** — v4 잔재 정리(②.5 목업 갤러리 제거, 다양성 게이트→골든 어휘 검사, QA 전담화),
   PIPELINE.md 갱신.
3. 콘텐츠 패스 이월분 — 용어 번역 오딧(내부 식별자 금지어), 장별 독자 질문 게이트.
4. 커밋 — 본 세션 산출물 전체가 미커밋 상태(feature/golden-deck).

## 여정 / 변경 이력 (과거 — 본문과 분리)

- 콘텐츠는 가상 프로젝트 → **K-IFRS 1115 실데이터**로 재전환했다(가상 수치로는 디자인 검증 불가).
- 구 5장(원인 4)은 S4에 흡수돼 삭제됐고, 구 20장(성과)은 S15와 재탕이라 삭제됐다 —
  Part Ⅵ는 간지·성과 장 없이 클로징만 남는다(골든셋=부품 창고라 정합 불요).
- S15는 덤벨 차트·워플 시안을 거쳐 3구역 해부(분해도+기록+실패 설명)로 확정됐다.
- 리랭커·임베딩 파이프라인 서사는 골든 콘텐츠(K-IFRS 프로젝트)의 이야기다 — 이 공장 자체의
  이력이 아니다.
- design-rules는 §8b~8g 덧붙임 구조였다가 Ⅰ·Ⅱ·Ⅲ부 체계로 재편됐다(규칙 보존 73/73 대조).
