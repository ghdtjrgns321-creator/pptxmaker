# 골든 덱 전환 — 결정과 진행 방식

2026-07-09 결정: **"증상 → 규칙 추가" 루프를 중단하고, 손으로 만든 골든 덱을 기준으로 공장을 역설계한다.**
성공 기준이 "게이트 통과"에서 "골든 덱과의 일치"로 바뀐다 — 이것이 수렴점이다.
세부 실행 계획(17장 구성·Phase·리스크)은 [05-golden-deck-plan.md](05-golden-deck-plan.md).

## 핵심 결정 (grilling 인터뷰로 확정)

| 항목         | 결정                                                                                   | 근거                                                                                 |
| ------------ | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| 방식         | 골든 덱 수동 제작 → 공장이 그것을 재현하도록 역설계                                    | 상향식 규칙 루프는 수렴점이 없음 ([02-failure-analysis.md](02-failure-analysis.md))  |
| 시각 무드    | 흰 본문 + 다크 간지 (현행 유지)                                                        | 인쇄·밝은 회의실 친화, 기존 brand-kit 재사용                                         |
| 형태         | 아키타입 카탈로그가 아니라 **완전한 견본 덱 17장** (실질 디자인 12종, 간지는 1종 반복) | 흐름 속 리듬·반복까지 기준이 되어야 함. 8장 카탈로그 안은 인터뷰에서 기각            |
| 콘텐츠       | 가상 프로젝트 "전표 리스크 스코어링 플랫폼" (K-IFRS 배제)                              | 회계·감사 AI 장르 유지, 17장 데이터 형태 전부 자연스러움                             |
| 레퍼런스     | 역할 분담 — 틀·톤=BCG·Dallas, 전시물 체급·숫자 무대=McKinsey, 장마다 모방 페이지 명시  | "고급스럽게"를 모방 대상 페이지로 구체화                                             |
| 재현 판정    | 구조 비교(도형 좌표·폰트·색 전수 M/N) + 최종 육안 1회                                  | 자동·결정적 — 재빌드마다 회귀 검사 가능                                              |
| 확장         | 풀(pull) 방식 — 실전 덱에서 미커버 형태가 나올 때만 1장 추가 (YAGNI)                   | 골든 슬라이드 1장 = 공장이 유지보수할 재현 계약 1개. 미리 늘리면 v4 규칙 더미의 재판 |
| 내용 고도    | 갭 #4(개발일지 고도)는 골든 덱과 별도 트랙 — deck-compose 단계 큐레이션으로 해결       | 서식 문제가 아니라 콘텐츠 문제                                                       |
| 기존 v4 장치 | 역설계 완료 후 간소화·재배치(②.5 목업 갤러리 제거, 다양성 게이트→골든 어휘 준수 검사)  | 게이트 이중화 방지. 완료 전에는 손대지 않음                                          |

## 진행 루프

```
[골든 덱 제작]                        [공장 역설계]                  [실전 검증]
golden/ 스크립트로 17장         ─▶  build_pptx에 레이아웃 이식  ─▶  K-IFRS 덱 재빌드로 갭 재점검
부 단위 제작 → 렌더 → 승인? ─┬─예       + 구조 비교 M/N 일치              │
                            └─아니오─▶ 해당 장 재디자인                  ▼
                                                          17장에 없는 형태 발견 시
                                                          그때만 골든 1장 추가 (pull)
```

- 골든 덱 제작: 공장 없이 golden/ 전용 python-pptx 스크립트로 한 장 한 장, 부 단위 묶음 8회 승인.
- 역설계: brand-kit·build_pptx가 골든 덱 재현 기준에 종속된다. 완료 후 golden/ 스크립트는 동결(기준 원본).
- 기존 QA 게이트는 폐기가 아니라 강등: 마감 결함(각주 겹침·잘림 등 갭 #7)처럼 규칙으로 잡히는 것만 담당한다.

## 결정 이력

- 초기 구상은 "아키타입 8장 카탈로그"였으나, 2026-07-09 grilling 인터뷰에서 사용자 목차 6부(문제 인식 → 파이프라인 → 기술 설명 → 문제와 해결 → 차별점 → 성과·증거)를 전부 흐르는 완전한 견본 덱으로 확정했다.
- 시각 무드(다크 단일 vs 흰 본문 + 다크 간지)는 같은 인터뷰에서 흰 본문 + 다크 간지로 해소됐다. 미결정 사항 없음.
