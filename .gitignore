# Python
__pycache__/
*.py[cod]
*.egg-info/
.ruff_cache/

# uv / venv
.venv/

# 빌드 산출물 (생성된 발표자료)
*.pptx
!**/assets/**/*.pptx

# 로컬 상태 (세션별 계약)
.claude/state/

# 파이프라인 작업물 (재료·중간 산출물 — 재생성 가능)
_workspace/
_workspace_prev/
_workspace_kifrs/

# 레퍼런스 원본 (대용량·저작물 PDF — 로컬 보관만)
data/
ref/

# OS
.DS_Store
Thumbs.db

# 렌더 중간 산출물·세션 상태·OS 잡동사니 (골든 시안 PNG는 golden/variants/*.png로 보존)
golden/.claude/
golden/render/
desktop.ini

# 골든 렌더 산출물 — build_golden.py로 재생성. 대표본은 docs/images/에 보존
golden/render_v6/
golden/_pilot_s06/render/
golden/_pilot_s06/_ref_crops/
golden/_proto_s06_f_render/
golden/backup/**/render/
